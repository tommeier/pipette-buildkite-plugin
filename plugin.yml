name: Pipette
description: Declarative Buildkite pipeline generation for monorepos, written in Elixir
author: https://github.com/tommeier
requirements: []
configuration:
  properties:
    pipeline:
      type: string
      description: "Path to the Elixir pipeline script (e.g., .buildkite/pipeline.exs)"
  required:
    - pipeline
